filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/honeypot/*.log
    # Cho log text
    # -> ko bật json parse

  - type: log
    enabled: true
    paths:
      - /var/log/honeypot/*.json
    json.keys_under_root: true
    json.overwrite_keys: true
